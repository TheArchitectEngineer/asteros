# Generated by build.sh's host-side codegen step (substads/preproc,
# same role as autoconf's config.status -- see build.sh's own comment)
# and by build.sh's XAWPLAIN/TOOLFLAGS-unset setup. Not shipped by
# upstream; regenerated fresh on every build.sh run.
XPaint.ad
XPaint.ad.h
DefaultRC.txt.h
messages.h
xaw_incdir
bitmaps/tools
