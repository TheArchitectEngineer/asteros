# vim: set expandtab shiftwidth=2 tabstop=8 textwidth=0 filetype=yaml:
#
# This CI uses the freedesktop.org ci-templates.
# Please see the ci-templates documentation for details:
# https://freedesktop.pages.freedesktop.org/ci-templates/

.templates_sha: &template_sha c117daf8216a5f586588cf6c14718e349df32cb3 # see https://docs.gitlab.com/ee/ci/yaml/#includefile


include:
  # Arch container builder template
  - project: 'freedesktop/ci-templates'
    ref: *template_sha
    file: '/templates/arch.yml'
  - project: 'freedesktop/ci-templates'
    ref: *template_sha
    file: '/templates/ci-fairy.yml'
  - template: Security/SAST.gitlab-ci.yml


stages:
  - prep             # prep work like rebuilding the container images if there is a change
  - build            # for actually building and testing things in a container
  - test
  - deploy


variables:
  FDO_UPSTREAM_REPO: 'xorg/app/xeyes'
  # The tag should be updated each time the list of packages is updated.
  # Changing a tag forces the associated image to be rebuilt.
  # Note: the tag has no meaning, we use a date format purely for readability
  FDO_DISTRIBUTION_TAG: '2026-06-28.0'
  # minimal set of packages required to build and install either way
  BASE_PACKAGES: 'git gcc clang pkgconf libxi libxfixes libx11 libxt libxext libxmu xorgproto libxrender libxcb'
  # packages needed to build and install with meson
  MESON_PACKAGES: 'meson ninja'
  FDO_DISTRIBUTION_PACKAGES: $BASE_PACKAGES $MESON_PACKAGES


#
# Verify that commit messages are as expected
#
check-commits:
  extends:
    - .fdo.ci-fairy
  stage: prep
  script:
    - ci-fairy check-commits --junit-xml=results.xml
  except:
    - master@xorg/app/xeyes
  variables:
    GIT_DEPTH: 100
  artifacts:
    reports:
      junit: results.xml


#
# Build a container with the given tag and the packages pre-installed.
# This only happens if when the tag changes, otherwise the existing image is
# re-used.
#
container-prep:
  extends:
    - .fdo.container-build@arch
  stage: prep
  variables:
    GIT_STRATEGY: none


#
# The meson build, runs on the image built above.
#
.meson_build:
  stage: build
  extends:
    - .fdo.distribution-image@arch
  script:
    - CC="${CC}" meson setup _builddir --prefix="$PWD/_install"
    - meson compile -C _builddir
    - meson test -C _builddir
    - meson install -C _builddir
    - ./_install/bin/xeyes -version
  artifacts:
    paths:
      - _builddir/config.h
      - _builddir/meson-logs/meson-log.txt

# Run meson build with different compilers
meson:
  extends:
    - .meson_build
  parallel:
    matrix:
      - CC: ["gcc", "clang"]
