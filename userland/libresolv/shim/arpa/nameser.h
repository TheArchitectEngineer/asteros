/* Real vendored libresolv (src/libresolv/nameser.h) ships this header
 * at its own repo root instead of under an arpa/ subdirectory. This
 * thin wrapper just exposes it at the <arpa/nameser.h> path every real
 * libresolv .c file expects. */
#include "../../../../src/libresolv/nameser.h"
